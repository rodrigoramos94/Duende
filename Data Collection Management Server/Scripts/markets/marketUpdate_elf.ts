import mongo = require('./../mongodb/mongodb')

// se ha creado provisionalmente
export var AvailableMarkets = [{
    name: 'Alphabet Inc.',
    symbol: 'GOOGL'
}, {
    name: 'Microsoft',
    symbol: 'MSFT'
    }
]

export function run() {
    setInterval(function () {

    }, 60000);
}